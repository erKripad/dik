//----------------------------------------------------------------------------//

// in questa pagina vengono specificate le variabili rappresentanti dei colori

// l'app sarà composta da diversi temi (chiaro, scuro, etc), è necessario specificare i colori nel caso di cambiamenti futuri

//----------------------------------------------------------------------------//

import 'package:flutter/material.dart';

//----------------------------------------------------------------------------//

// colori principali dell'app (tema scuro)

// creare effetto per la bottom bar che quando scorre svaniscono le schedfe

// fare l'app bar con lo stesso container della bottombar

const Color primaryBackgroundColor = Color.fromARGB(255, 24, 23, 28);
Color primaryObjColor = const Color.fromARGB(255, 45, 44, 48).withOpacity(0.8);
const Color primaryPurple = Color.fromARGB(255, 165, 126, 255);
const Color textColor = Color.fromARGB(255, 255, 255, 255);
const Color iconColor = Color.fromARGB(255, 255, 255, 255);

//----------------------------------------------------------------------------//
